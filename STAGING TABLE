  CREATE TABLE "STAGING" 
   (	"ID" NUMBER(14,0)  INCREMENT BY 1 START WITH 1 NOT NULL ENABLE, 
	"NAME" VARCHAR2(20), 
	"ADDRESS" NVARCHAR2(20), 
	"STARTDATE" DATE DEFAULT SYSDATE NOT NULL ENABLE, 
	 CONSTRAINT "STAGING_PK" PRIMARY KEY ("ID")
  USING INDEX  ENABLE
   ) ;
